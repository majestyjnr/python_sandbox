# A loop iterates over a sequence

# While loops executes a set of statements as long as the condition is true

names = ['Solomon', 'Prince', 'Henry', 'Daniel', 'Samuel', 'Charles']

# Simple For Loop
for person in names:
    print(f'Current Person is:  {person}')